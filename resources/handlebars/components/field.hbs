<label class="admin-login__field admin-field {{#if errors}} admin-danger {{/if}}">
    {{#if label}}<span class="admin-field__text">{{label}}</span>{{/if}}
    <input class="admin-login__field__input admin-field__input"
        {{#if type}} type="{{type}}" {{/if}}
        {{#if name}} name="{{name}}" {{/if}}
        {{#if value}} value="{{value}}" {{/if}}
        {{#if readonly}} readonly {{/if}}
    >
    {{#if errors}}
        {{#each errors}}
            <span class="admin-text--mini admin-text--danger">{{this}}</span>
        {{/each}}
    {{/if}}
</label>